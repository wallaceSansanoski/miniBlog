const DisplaySearch = ({title, createAt, content, tags, image}) => {

    console.log(title, createAt, content, tags, image)
    return (
        <div>

        </div>
    )
}

export default DisplaySearch;